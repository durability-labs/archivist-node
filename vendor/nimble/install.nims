# Initializes submodules in vendor/nimble, and performs installation for those
# nimble packages that need it.

import "./install_submodules.nims"
import "./install_nat_traversal.nims"

{.warning[UnusedImport]: off.}
